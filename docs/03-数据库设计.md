# 自动化运维平台 V1 数据库设计

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v2.2（竣工审查：同步执行范式改造 0007/0008 与凭据集成 0009——删 ticket_host/execution_step_host，模板/工单改单选作业主机） |
| 状态 | 竣工（与代码同步） |
| 上游文档 | [01-PRD](./01-产品需求文档PRD.md)、[02-技术架构设计](./02-技术架构设计.md) |
| 数据库 | MySQL 8.0，库名 `opspilot`，字符集 `utf8mb4` / `utf8mb4_0900_ai_ci`，引擎 InnoDB；共 **26 张表**（登录鉴权 6 + CMDB 3 + 作业 6 + 工单 3 + 执行 2 + 审计系统 2 + 通知 4） |

## 0. 设计约定

- 主键统一 `id BIGINT UNSIGNED AUTO_INCREMENT`；表名蛇形单数；外键**不建物理外键约束**（应用层保证 + 索引），避免锁与迁移负担。
- 通用字段：`created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3)`、`updated_at DATETIME(3) ON UPDATE CURRENT_TIMESTAMP(3)`（快照类/日志类表不加 updated_at）。
- 枚举一律 `VARCHAR` 存字符串常量（代码内 Enum 管理），避免 MySQL ENUM 迁移痛点。
- 加密字段后缀 `_enc`（AES-256-GCM，base64 存储）；快照字段后缀 `_snap` 或所在表本身即快照表。
- 逻辑删除仅用于 `user`（`status=disabled`）；其余业务表物理删除 + 删除保护校验 + 审计。

## 1. ER 总览

```
user ─┬─< user_role >─ role ─< role_permission >─ permission
      ├─< api_token          (个人访问密钥)
      └─< ticket(creator) 

host ──< app_host >── application   (CMDB 资产台账，与执行链路解耦)

credential ──被引用── job_host      (作业主机 = 统一执行节点)
                          ▲ (模板单选作业主机)
ticket_template ─┬─< template_step          (步骤内嵌脚本)
                 ├─< template_approval_node (模板内审批节点)
                 ├─< template_version       (全量配置快照版本)
                 └── 快照引用 ── ticket ─< ticket_step   (步骤内容快照)
                                      │ ─< ticket_approval (审批记录)
                                      ▼
                                   execution ─< execution_step
notify_channel ─< notify_channel_event
notification_record
user_notification
audit_log (按月分区)
system_config
```

## 2. 登录鉴权模块

### 2.1 `user` 用户

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| username | VARCHAR(64) | UK `uk_username` | 登录名 |
| password_hash | VARCHAR(255) | NULL | bcrypt；LDAP/OIDC 用户为空 |
| display_name | VARCHAR(64) | | 姓名 |
| email | VARCHAR(128) | | 通知收件地址 |
| source | VARCHAR(16) | | `local / ldap / oidc` |
| external_id | VARCHAR(128) | NULL | LDAP DN / OIDC sub |
| status | VARCHAR(16) | | `active / disabled` |
| mfa_enabled | TINYINT(1) | DEFAULT 0 | 是否已绑定 |
| mfa_secret_enc | VARCHAR(512) | NULL | TOTP 密钥密文 |
| must_change_password | TINYINT(1) | DEFAULT 0 | 初始/重置后首登改密 |
| locked_until | DATETIME(3) | NULL | 登录失败锁定截止 |
| last_login_at | DATETIME(3) | NULL | |

索引：`uk_username(username)`、`idx_source_external(source, external_id)`。

### 2.2 `role` 角色

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| code | VARCHAR(32) | UK | `admin/ops/approver/auditor/自定义` |
| name | VARCHAR(64) | | 显示名 |
| is_builtin | TINYINT(1) | | 内置角色不可删 |
| description | VARCHAR(255) | | |

### 2.3 `permission` 权限点

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT UN | PK |
| code | VARCHAR(64) UK | 如 `cmdb:write`、`ticket:approve`（共 30 个权限点） |
| name | VARCHAR(64) | 显示名 |
| module | VARCHAR(32) | 所属模块（菜单分组用） |

代码内常量为唯一事实来源，启动时幂等同步入库。

### 2.4 `user_role` / `role_permission` 关联

| 表 | 字段 | 约束 |
| --- | --- | --- |
| user_role | user_id, role_id | UK(user_id, role_id)，idx(role_id) |
| role_permission | role_id, permission_id | UK(role_id, permission_id) |

> Refresh Token 吊销、登录失败计数、权限缓存均在 Redis，不建表。

### 2.5 `api_token` 个人访问密钥

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| user_id | BIGINT UN | | 所属用户 |
| name | VARCHAR(64) | | 密钥名称 |
| token_hash | VARCHAR(64) | UK | 令牌 SHA-256 哈希（不存明文） |
| prefix | VARCHAR(16) | | 展示用前缀（`opsp_xxxx`） |
| expires_at | DATETIME(3) | NULL | 过期时间（空=永不过期） |
| last_used_at | DATETIME(3) | NULL | 最后使用时间（60 秒节流更新） |
| created_at | DATETIME(3) | | |

索引：`uk(token_hash)`、`idx_api_token_user_id(user_id)`。

- 明文格式 `opsp_` + `token_urlsafe(32)`，仅创建成功时返回一次；库中仅存 SHA-256 哈希。
- 每人上限 10 个；删除密钥 / 禁用账号后密钥立即失效。

## 3. CMDB 模块

### 3.1 `host` 主机

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| hostname | VARCHAR(128) | | 主机名 |
| ip | VARCHAR(45) | UK `uk_ip` | 管理 IP（支持 IPv6 长度） |
| platform | VARCHAR(64) | NULL | 所属平台（自由文本+补全） |
| region | VARCHAR(64) | NULL | 所属区域 |
| os | VARCHAR(64) | NULL | 操作系统（自由文本，迁移 0002 新增） |
| cpu_cores | INT | NULL | CPU 核数（0002 新增） |
| memory_gb | INT | NULL | 内存 GB（0002 新增） |
| disk_gb | INT | NULL | 磁盘 GB（0002 新增） |
| environment | VARCHAR(16) | | `demo / stage / prod` |
| status | VARCHAR(16) | DEFAULT online | `online / offline / maintenance` |
| ssh_port | INT | DEFAULT 22 | 连接端口 |
| description | VARCHAR(255) | NULL | 说明 |
| created_by | BIGINT UN | NULL | 创建人 user.id |

索引：`uk_ip(ip)`、`idx_host_environment(environment)`、`idx_host_status(status)`、`idx_host_platform_region(platform, region)`。

### 3.2 `application` 应用服务

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| name | VARCHAR(128) | UK | 应用名 |
| description | VARCHAR(255) | NULL | |
| language | VARCHAR(32) | | 自由文本 |
| deploy_type | VARCHAR(16) | | `shell / docker / k8s` |
| created_by | BIGINT UN | | |

### 3.3 `app_host` 应用-主机关联（多对多）

| 字段 | 类型 | 约束 |
| --- | --- | --- |
| id | BIGINT UN | PK |
| app_id | BIGINT UN | UK(app_id, host_id) |
| host_id | BIGINT UN | idx(host_id) |

删除保护：删主机前查 `app_host`（被应用关联则拒绝）。执行范式改造后工单引用的是作业主机（`job_host`），与 CMDB 主机解耦，主机/应用均无工单侧删除保护；删应用时级联清理 `app_host` 关联。

## 4. 作业执行模块

### 4.1 `credential` SSH 凭据

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| name | VARCHAR(64) | UK | 引用名 |
| login_user | VARCHAR(64) | | SSH 登录用户 |
| auth_type | VARCHAR(16) | | `password / private_key` |
| secret_enc | TEXT | | 密码或私钥密文 |
| passphrase_enc | TEXT | NULL | 私钥口令密文 |
| description | VARCHAR(255) | | |
| created_by | BIGINT UN | | |

删除保护：被作业主机（`job_host.credential_id`）或模板引用（`ticket_template.credential_refs`）的凭据不可删除（42201）。

### 4.1a `job_host` 作业主机（执行范式改造后的统一执行节点）

登录认证随关联凭据（凭据管理），主机侧不再存储任何账号/密文字段（迁移 0009 已删 `login_user / auth_type / secret_enc / passphrase_enc` 四列）。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| name | VARCHAR(128) | UK | 作业主机名称 |
| ip | VARCHAR(45) | UK(ip, ssh_port) | |
| ssh_port | INT | DEFAULT 22 | |
| credential_id | BIGINT UN | NULL | 关联凭据 `credential.id`（API 层必填；建连账号与密文实时取自凭据） |
| workdir | VARCHAR(255) | DEFAULT /opt/opspilot/workspace | 脚本落盘与执行根目录 |
| enabled | TINYINT(1) | DEFAULT 1 | 禁用后不可被新模板选用 |
| last_check_at / last_check_ok / last_check_msg | — | NULL | 最近连通性测试结果 |
| created_by | BIGINT UN | | |

### 4.2 `ticket_template` 工单模板（规则定义，模板管理维护）

所有工单规则（作业主机/步骤/策略/审批/通知/权限范围/凭据引用）均配置在模板内部，**无全局审批流**。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| name | VARCHAR(128) | UK | 模板名（工单标题直接使用模板名） |
| type | VARCHAR(16) | | `release / daily_ops / other` |
| description | VARCHAR(512) | | |
| job_host_id | BIGINT UN | idx `idx_ticket_template_jh` | 作业主机（单选；迁移 0007 由 app_id 改造而来） |
| exec_strategy | JSON | | `{timeout, fail_fast}` |
| approval_enabled | TINYINT(1) | DEFAULT 1 | 是否需要审批（每模板独立） |
| allow_withdraw | TINYINT(1) | DEFAULT 1 | 是否允许创建人撤回 |
| allow_transfer | TINYINT(1) | DEFAULT 0 | 是否允许转交（V1 仅存配置，不生效） |
| allow_countersign | TINYINT(1) | DEFAULT 0 | 是否允许加签（V1 仅存配置，不生效） |
| notify_rules | JSON | | `[{event, receivers, channels}]`；receivers 支持 `creator / approver_role / role:<id>` |
| visible_role_ids | JSON | | 可使用此模板提交的角色 id 数组；空数组=所有具备 `ticket:write` 的角色 |
| credential_refs | JSON | NULL | 引用凭据声明 `[{alias, credential_id}]`；shell 步骤执行时注入 `CRED_<ALIAS大写>_USER/_SECRET/_PASSPHRASE` 环境变量；变更触发升版 |
| status | VARCHAR(16) | | `enabled / disabled`；仅启用模板可被提交 |
| current_version | INT | | 指向最新版本号 |
| created_by | BIGINT UN | | |

### 4.3 `template_step` 模板步骤（脚本内嵌，一模板多步）

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| template_id | BIGINT UN | UK(template_id, step_order) | |
| step_order | INT | | 1..N，严格串行 |
| name | VARCHAR(128) | | 步骤名 |
| script_type | VARCHAR(16) | | `shell / playbook` |
| content | MEDIUMTEXT | | 脚本/Playbook 内容（含 `{{ 变量 }}`） |
| params_schema | JSON | | `[{name,label,default,required,fixed,description}]`；`fixed=true` 的参数锁定默认值，提交人不可见不可改 |
| timeout | INT | DEFAULT 600 | 步骤超时秒（迁移 0008 已删本步骤 credential_id 列，登录凭据随作业主机） |

### 4.4 `template_approval_node` 模板审批节点（每模板独立审批流）

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| template_id | BIGINT UN | UK(template_id, node_order) | |
| node_order | INT | | 1..10，节点间依次推进 |
| role_id | BIGINT UN | | 该节点审批角色 |
| approve_mode | VARCHAR(8) | DEFAULT 'any' | `any(或签) / all(会签) / seq(节点内依次)`；V1 仅 `any` 生效（角色内任一成员处理即定节点结果），其余存配置 |

### 4.5 `template_version` 模板版本（全量配置快照，不可变）

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| template_id | BIGINT UN | UK(template_id, version) | |
| version | INT | | 从 1 递增 |
| snapshot | JSON | | 全量配置快照（基本信息+步骤+策略+审批节点+通知+权限范围） |
| changelog | VARCHAR(500) | | |
| created_by | BIGINT UN | | |
| created_at | DATETIME(3) | | |

步骤/策略/审批/通知/参数任一变更 = 插入新版本 + 更新 `current_version`；版本行永不修改。被进行中工单引用的模板可编辑（工单用快照），但删除受保护。

## 5. 工单中心模块

### 5.1 `ticket` 工单（实例：基于模板某版本的一次具体提交）

工单中心只能使用模板：提交时仅填写参数，标题使用模板名，其余全部来自模板快照。**无草稿态，提交即生效**。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT UN | PK | |
| ticket_no | VARCHAR(32) | UK | 单号 `T20260727-0001` |
| template_id | BIGINT UN | idx | 源模板（溯源） |
| template_version_snap | INT | | 提交时模板版本号快照 |
| title | VARCHAR(200) | | 提交时模板名快照（标题=模板名） |
| type | VARCHAR(16) | | 模板类型快照 `release / daily_ops / other` |
| params | JSON | | 提交人填写的汇总参数 `{name: value}`（各步骤同名参数合并） |
| job_host_id | BIGINT UN | | 作业主机（来自模板） |
| job_host_snap | JSON | | 作业主机快照 `{id, name, ip, ssh_port, workdir}` |
| status | VARCHAR(16) | idx | 过程态 `approving / queued / running / paused` + 终态 `success / failed / rejected / cancelled / interrupted` |
| interrupt_reason | VARCHAR(32) | NULL | 终态为 interrupted 时的起因：`user_abort`（用户中止）/ `system_crash`（Worker 崩溃）/ `worker_lost`（队列重试耗尽）；预留 infra_failure/unknown |
| exec_strategy_snap | JSON | | 执行策略快照（来自模板） |
| flow_snap | JSON | | 审批节点快照 `[{node, role_id, role_name, approve_mode}]`；免审为 `[]` |
| credential_refs | JSON | NULL | 提单时冻结的模板引用凭据 `[{alias, credential_id, credential_name}]`；执行时按 credential_id 实时取密文，快照不落任何密文 |
| allow_withdraw_snap | TINYINT(1) | | 是否允许撤回（来自模板） |
| current_node | INT | DEFAULT 0 | 当前审批节点（0=未进入/已结束） |
| creator_id | BIGINT UN | idx | |
| submitted_at / finished_at | DATETIME(3) | NULL | |

索引：`idx_ticket_status(status)`、`idx_ticket_creator(creator_id, created_at)`、`idx_ticket_template(template_id)`。

状态机（应用层校验合法迁移，无 draft/pending；paused 为非终态）：
`approving → queued → running → success/failed`；`approving → rejected / cancelled(撤销)`；`running ↔ paused`（暂停/恢复）；`queued/running/paused → interrupted`（中止或系统崩溃，起因落 `interrupt_reason`）。模板免审时提交直接进入 `queued`（入执行队列），Worker 认领后置 `running`。语义边界：`interrupted`=已进入执行链路但未跑完（≠ failed 真实失败，≠ cancelled 审批前作废），人为停止不得计入 failed。

### 5.2 `ticket_step` 工单步骤（提交时从模板固化的内容快照）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT UN | PK |
| ticket_id | BIGINT UN | UK(ticket_id, step_order) |
| step_order | INT | 1..N，严格串行 |
| step_name_snap | VARCHAR(128) | 步骤名快照 |
| script_type_snap | VARCHAR(16) | `shell / playbook` |
| content_snap | MEDIUMTEXT | 提交时脚本内容快照 |
| params | JSON | 本步骤生效参数值 `{name: value}`（固定值+提交人填写合并后） |
| timeout | INT | 步骤超时秒（来自模板；迁移 0008 已删本步骤 credential_id 列） |

### 5.3 `ticket_approval` 审批记录

审批规则定义在模板内（§4.4），本表仅记录实例流转。**无全局审批流表**。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT UN | PK |
| ticket_id | BIGINT UN | idx(ticket_id) |
| node_order | INT | 审批节点序号 |
| role_id | BIGINT UN | 快照 |
| approver_id | BIGINT UN | 实际审批人（角色任一成员；可审批自己创建的工单） |
| action | VARCHAR(16) | `approve / reject` |
| comment | VARCHAR(512) | 驳回必填 |
| created_at | DATETIME(3) | |

## 6. 执行记录模块

### 6.1 `execution` 执行实例（1 工单 : 1 执行，V1）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT UN | PK |
| ticket_id | BIGINT UN | idx |
| status | VARCHAR(16) | `queued / running / paused / success / failed / terminated / interrupted / rejected / cancelled` |
| total_steps | INT | 步骤总数 |
| triggered_by | VARCHAR(16) | 触发来源，默认 `approval`（审批通过/免审自动入队） |
| started_at / finished_at | DATETIME(3) | |

状态语义：`terminated` = 人为终止（映射工单 `interrupted + user_abort`）；`interrupted` = 系统崩溃/失联（映射 `system_crash / worker_lost`）；`cancelled` = 工单撤回后关闭排队实例；运行中的工单不允许撤回。工单终态映射表见 02-架构 §4.2。

### 6.2 `execution_step` 步骤执行

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT UN | PK |
| execution_id | BIGINT UN | UK(execution_id, step_order) |
| ticket_step_id | BIGINT UN | 引用步骤定义 |
| step_order | INT | |
| status | VARCHAR(16) | `pending / running / paused / success / failed / skipped / terminated` |
| exit_code | INT NULL | 退出码（Shell / ansible-playbook 进程退出码） |
| error_summary | VARCHAR(512) | 失败摘要（完整日志在文件） |
| started_at / finished_at | DATETIME(3) | |

日志文件路径由约定生成：`/data/logs/{execution_id}/step_{step_order}.log`（每步骤一个文件），不入库。

容量估算：1000 工单/年 × 3 步 ≈ 3000 行/年（execution_step），单表极轻量；`uk_execution_step(execution_id, step_order)` 保证详情页查询效率。

## 7. 通知模块

### 7.1 `notify_channel` 渠道配置（全局一份）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT UN | PK |
| type | VARCHAR(16) UK | `email / webhook / teams / dingtalk / feishu / wecom` |
| enabled | TINYINT(1) | |
| config | JSON | 非敏感配置（服务器、URL、发件人等）；消息模板存于 `templates` 对象，按事件保存：Email `{title, content}`、Webhook `{body}`、Teams `{card}`；未配置事件使用后端渠道×事件默认模板 |
| secret_enc | TEXT NULL | 敏感项密文（SMTP 密码 / Webhook 签名密钥） |
| updated_by | BIGINT UN | |

### 7.2 `notify_channel_event` 事件-渠道映射

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT UN | PK |
| event | VARCHAR(64) | `ticket.pending_approval / ticket.approved / ticket.rejected / execution.success / execution.failed / execution.interrupted` |
| channel_type | VARCHAR(16) | UK(event, channel_type) |

### 7.3 `notification_record` 发送记录

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT UN | PK |
| event | VARCHAR(64) | idx(event, created_at) |
| channel_type | VARCHAR(16) | |
| receiver | VARCHAR(255) | 邮箱/URL 摘要 |
| title | VARCHAR(255) | |
| content | TEXT | 渲染后内容 |
| ref_type / ref_id | VARCHAR(16) / BIGINT | 关联对象（ticket/execution） |
| status | VARCHAR(16) | `pending / success / failed` idx(status) |
| retry_count | INT DEFAULT 0 | |
| next_retry_at | DATETIME(3) NULL | 退避调度 |
| error | VARCHAR(512) | |
| sent_at | DATETIME(3) NULL | |

### 7.4 `user_notification` 站内信（NOTIFY-06）

事件发射时与发送记录同事务落库：按收件人用户名解析为有效用户，每人一行；
不受渠道启用/事件映射影响，无发送/重试概念；解析不到的用户名静默跳过。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT UN | PK |
| user_id | BIGINT UN | 收件用户；idx(user_id, is_read)、idx(user_id, created_at) |
| event | VARCHAR(64) | 触发事件编码 |
| title | VARCHAR(255) | 业务默认文案（不走渠道模板） |
| content | TEXT NULL | |
| ref_type / ref_id | VARCHAR(16) / BIGINT NULL | 关联对象（ticket/execution，铃铛点击跳转） |
| is_read | TINYINT(1) DEFAULT 0 | 已读标记 |
| read_at | DATETIME(3) NULL | |
| created_at | DATETIME(3) | |

## 8. 安全审计模块

### 8.1 `audit_log`（按月 RANGE 分区，只 INSERT/SELECT）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT UN | 与 created_at 组成复合主键（分区要求） |
| created_at | DATETIME(3) | PK 组成部分，分区键 |
| actor_id | BIGINT UN NULL | 操作人（登录失败可为空） |
| actor_name | VARCHAR(64) | 冗余快照 |
| source_ip | VARCHAR(45) | |
| module | VARCHAR(32) | `auth / user / role / cmdb / credential / template / ticket / approval / execution / notify / audit / system` |
| action | VARCHAR(64) | 如 `login.success`、`host.import`、`ticket.approve` |
| target_type | VARCHAR(32) | 对象类型 |
| target_id | VARCHAR(64) | 对象标识 |
| target_name | VARCHAR(128) | 对象名快照 |
| result | VARCHAR(16) | DEFAULT `success`；`success / failed` |
| detail | JSON | 变更前后摘要/上下文 |

```sql
PRIMARY KEY (id, created_at)
KEY idx_audit_actor_time (actor_id, created_at),
KEY idx_audit_module_action (module, action)
PARTITION BY RANGE COLUMNS(created_at) (
  PARTITION p202601 VALUES LESS THAN ('2026-02-01'), ...  -- 迁移 0001 预建 2026-01~2028-12 月分区 + pmax，清理任务 DROP 过期整月分区
);
```

## 9. 系统配置

### 9.1 `system_config`（KV，JSON 值）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| cfg_key | VARCHAR(64) PK | |
| cfg_value | JSON | 敏感字段在应用层加密后存入 |
| updated_by | BIGINT UN | |
| updated_at | DATETIME(3) | |

预置 key：

| key | 内容 |
| --- | --- |
| `mfa.policy` | `off / optional / required` |
| `token.policy` | 令牌时效覆盖 `{access_minutes, refresh_days}`（空=用 .env 默认） |
| `password.policy` | 长度/复杂度/锁定阈值 |
| `ldap.config` / `oidc.config` | SSO 配置（bind 密码、client_secret 加密） |
| `exec.global_concurrency` | 全局 SSH 并发上限（默认 50） |
| `ldap.default_role` / `oidc.default_role` | SSO 首登默认角色 |

> 审计保留天数 `AUDIT_RETENTION_DAYS` 按 PRD 要求放 `.env` 配置文件，不入库。

## 10. 种子数据（api 启动 lifespan 内幂等执行，GET_LOCK 串行化）

1. 权限点全量 30 个（代码常量 `constants.PERMISSIONS` 同步）；开发阶段允许清理并按最新全集重建权限数据。
2. 内置角色 `admin/ops/approver/auditor` 及其权限绑定（按 PRD §2.2 矩阵，矩阵只增不减）。
3. 初始用户 `admin`（密码取 `ADMIN_INITIAL_PASSWORD`，未配置则随机生成并打印到容器日志，`must_change_password=1`）。
4. `system_config` 8 个预置键默认值（只补缺失不覆盖；作业主机改独立 `job_host` 表管理，废弃键 `ansible.job_host` 不再预置）。
5. 通知默认值：`notify_channel` 建 6 个类型占位行（email/webhook/teams/dingtalk/feishu/wecom，全部禁用）；`notify_channel_event` 为 6 个事件 × 3 个落地渠道（email/webhook/teams）建 18 条默认映射。

> 审批规则随模板配置，不预置默认审批流种子；0003 迁移已删除废弃键 `approval.enabled`。
